
interface Props{
  params: { id: string };
  searchParams: { genre: string };
}

export const GenrePage = ( { params: { id } , searchParams: { genre } } :Props) => {
  return (
    <div>

    </div>
  )
}
